{
  "name": "backend",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "dependencies": {
    "@cap-js/postgres": "^1.14.2",
    "@sap/cds": "^8",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1"
  },
  "scripts": {
    "start": "cds-serve",
    "deploy": "cds deploy"
  },
  "private": true,
  "cds": {
    "requires": {
      "db": {
        "kind": "postgres",
        "credentials": {
          "url": "$DATABASE_URL"
        }
      },
      "auth": {
        "kind": "mocked",
        "users": {
          "sebas": {
            "password": "123",
            "roles": [
              "admin"
            ]
          }
        }
      }
    }
  }
}